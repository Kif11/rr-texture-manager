import pathlib

class RRTextureManager(object):

    def __init__(self):
        self.original_paths = []
        self.temp_paths = []

    @property
    def count_paths(self, arg):
        pass

    def add_original_path(self):
        pass

    def get_temp_paths(self):
        # idx= orgPath.index(oldPath)
        # if ( idx>=0):
        # return changedPath[idx]
        # else return oldPath
        pass

    def convert_all_paths(self):
        # for path in orgPath do:
        # newP= os.path.basename(path)+ "tmp\" +os.path.dirname(path)
        # changedPath.append(newP)
        pass
